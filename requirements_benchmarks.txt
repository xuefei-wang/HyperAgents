requests==2.32.4
# Fixed: `dotenv` is the wrong package; the intended one is `python-dotenv`.
# Pinned to match the rest of this file (and the main repo's installed
# version); see #863.
python-dotenv==1.2.2
tqdm==4.67.1
backoff==2.2.1
matplotlib==3.10.3
docker==7.1.0
datasets==3.6.0
GitPython==3.1.44
# Reconciled with requirements.txt (was 1.74.9) so both HA paths resolve the
# same litellm; matches the main pyproject.toml floor (`litellm>=1.83`). See #863.
litellm==1.83.11
pandas==2.3.2
sympy==1.14.0
